<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Viền -->
    <stroke
        android:width="2dp"
        android:color="#000000"/>
    <!-- Góc bo tròn (tùy chọn) -->
    <corners
        android:radius="8dp" /> <!-- Độ bo góc -->
    <!-- Màu nền (tùy chọn) -->
</shape>